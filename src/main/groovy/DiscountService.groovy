class DiscountService {

	/**
	 * calculates the total discount for the given products
	 * each product has the discount field which represents the procentual discount from the product price
	 * if the products list has more than 6 products, then there is an extra discount of 5 % added to the total discount
	 */
	def calculateDiscount(products) {

	}
}
